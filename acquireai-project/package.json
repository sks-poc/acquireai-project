{
  "name": "acquireai-natural-language-betting-agent",
  "version": "1.0.0",
  "private": true,
  "description": "AcquireAI hackathon prototype: responsible natural language betting recommendation agent.",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "dev": "concurrently \"cd backend && npm run dev\" \"npm run dev --prefix frontend\"",
    "test": "npm test --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "openai": "^6.35.0"
  }
}
